info:
  name: Get by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/tickets/{{ticketId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
