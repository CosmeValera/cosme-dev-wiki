info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/tickets"
  body:
    type: json
    data: '{"tableNumber":4,"items":"2x pizza","total":24.5,"paid":false}'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
